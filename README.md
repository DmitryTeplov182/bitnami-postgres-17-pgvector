# bitnami-postgres-17-pgvector

This image is based on Bitnami PostgreSQL 17 and includes pgvector, so after `CREATE EXTENSION vector;` you can use vector types and operators.

Docker images: https://hub.docker.com/repository/docker/dteplov182/bitnami-postgres-17-pgvector